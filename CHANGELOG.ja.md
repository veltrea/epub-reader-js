# 変更履歴

English: [CHANGELOG.md](CHANGELOG.md)

版番号は [セマンティックバージョニング](https://semver.org/lang/ja/)に従います。人に見せる
版番号は手で上げ、`src-tauri/build-number` のビルド番号は `scripts/package-macos.sh` が
配布物を作るたびに進めます。

## 未公開

### 変更

- **ライセンスを BSD-3-Clause から MIT に変えました。** 同梱しているもの（foliate-js・zip.js・
  fflate・Tauri など）はどれも縛りの少ないライセンスです。1.0.0 までに配った版は、
  BSD-3-Clause のままです。

## 1.0.0 — 2026-09-13

### 追加

- **Windows 11 で動くようになりました。** これまでは macOS だけでした。
  本を開いて縦書きで読む、しおりを付ける、検索する、設定を変える、書棚に戻る——
  ひととおりが Windows で動きます。**画面の文字も日本語で出ます。**
  インストーラ（`.exe` と `.msi`）も作れます。
  - **縦書きは macOS と同じように組まれます。** ルビ・縦中横・圏点も効きます。
    engine が違う（macOS は WebKit、Windows は Chromium）ので、ルビの大きさが
    わずかに違いますが、読むぶんには変わりません。
  - **まだ確かめていないことがあります。** ドラッグ&ドロップでの取り込み、
    ファイルを選ぶ窓、スリープとシャットダウン、読み上げエンジンの起動です。
    詳しくは README の「できていないこと」を見てください。
  - **見た目の作法はまだ macOS のままです。** アプリ名のメニューが「ファイル」の
    左にあり、Windows の普通の並びとは違います。

- **本文の一箇所だけに効く読みを登録できるようにしました。** 本文で語を選んで右クリックし
  「この場所だけの読みを登録」を選ぶと、その場所を覚えた登録になります。**場所は目印として
  使い、実際に読み替える所は表記の一致で決めます。** 文字の数え方は道具ごとに違うので、
  位置をそのまま信じると 1 文字ずれた所を読み替えてしまいます。そこで指定した位置の近くに
  ある同じ表記を探し、いちばん近い 1 つだけを読み替えます。近くに同じ語が 2 つあっても、
  指定していない方は変わりません。探す範囲（前後の文字数。既定 40）は編集画面で変えられます。
- **前後の文字を、平易な言葉で条件にできるようにしました。** 編集画面の「直前が」「直後が」
  から「ひらがな」「漢字以外」などを選ぶだけで、正規表現を書かずに指定できます。
  **一文字の漢字を登録すると熟語まで読み替えてしまう**という困りごとが、これで防げます。
  「行」に「直前が漢字以外」を付ければ、「銀行」「旅行」はそのままで、単独の「行」だけを
  読み替えます。中では後読み・先読みに組み立てています。
- **パターンで前後を条件にできるようにしました（後読み・先読み）。** 表記に
  `(?<=[ぁ-ん])行った` と書くと「直前がひらがなのときだけ」読み替えます。
  `(?<![一-龥])行` なら「直前が漢字でないときだけ」なので、「銀行」「旅行」を巻き込まずに
  単独の「行」だけを読み替えられます。**一文字の漢字を登録すると熟語まで変わってしまう**
  という困りごとは、この書き方で防げます。以前も書けましたが効きませんでした。一致した
  部分だけを取り出してもう一度当て直す作りだったため、前後の文字が失われて条件が必ず
  外れていました。
- **パターンの読みで、捕まえた部分をカタカナに直すようにしました。** 表記に
  `([ぁ-ん])行った`、読みに `$1オコナッタ` と書くと、「とり行った」が「トリオコナッタ」に
  なります。以前は捕まえた部分がひらがなのまま残っていました。`$&`（一致した全体）も
  使えるようにしました。これで「直前がひらがなのときだけ、その字を残して漢字の所を
  読み替える」が書けます。
- **読み上げ辞書を「すべての本」と「この本だけ」の 2 つに分けました。** 登場人物の名前の
  ように、その作品でしか使わない読みを、他の本に持ち込まずに登録できます。同じ表記が
  両方にあるときは「この本だけ」が勝ち、それを消すと「すべての本」の読みに戻ります。
  1 件の編集画面に「適用範囲」があり、あとから片方へ移せます。本を開いていないとき
  （書棚の画面）は「すべての本」だけが出ます。
- **読み上げ辞書の「登録する画面」と「登録済みを見る画面」を分けました。** 今までは 1 つの
  画面が登録も編集も兼ねていて、登録済みの全部がその場に並んでいました。一語だけ登録したい
  ときも長い一覧を通ることになり、画面を縦に何度も送る必要がありました。これからは、
  本文からの登録もメニューの **読み上げ > 読み上げ辞書に登録…** も、**表記・読み・
  オプションだけの画面**を出します。登録済みの一覧は出ません。「完了」を押すと保存して
  閉じます。登録済みを見て直したいときは、メニューの **読み上げ > 読み上げ辞書を編集…**
  を選びます（登録する画面の下にある「登録済みの一覧を見る」からも移れます）。
- **前に登録した語をもう一度登録しようとすると、前に入れた読みが入った状態で開きます。**
  新しく空の 1 件を作りません。同じ表記が二重に並ぶと、どちらが効いているのか分からなく
  なるためです。このときの見出しは「登録済みの読みを直す」になります。探す順は読み上げと
  同じで、「この本だけ」を先に見て、無ければ「すべての本」を見ます。
- **どちらの辞書へ入れるかを、登録する画面で選べるようにしました。** 「適用範囲」に
  「すべての本」と「この本だけ」が並んでいるので、片方を押すだけです。今までは
  「一覧のどちらの節にある＋を押したか」で決まっていました。
- **読み上げエンジンを自動で起動するようにしました。** 読み上げを押したときに VOICEVOX か
  AivisSpeech が動いていなければ、アプリがそのまま起動して、声を出せる状態になるまで待ちます。
  起動には十数秒かかることがあり、その間は画面下に「読み上げエンジンを起動しています…」と出ます。
  自動で起動できるのは VOICEVOX と AivisSpeech だけです。設定で「カスタム URL」を選んでいる
  ときは、どのアプリを起動すればよいか決められないので、今までどおり自分で起動してください。

### 修正

- **Windows で、文字サイズ・行間・テーマの設定が本文に届かない不具合を直しました。**
  設定を変えても本文の見た目が変わらない状態でした。
  Windows の画面部品（WebView2）が、あとから作った指定の入れ物を無視するのが原因です。
  別の入れ方に替えて直しました。**macOS では前から正しく効いていました。**
- **テストバスが、画面を読み込み直したあと二度とつながらなくなることがあったのを
  直しました。** 読み込み直した直後はバックエンドへの経路がまだ整っておらず、
  「テストバスを使ってよいか」の問い合わせが 1 回失敗することがあります。そこで諦めて
  いたため、そのページでは外から一切さわれなくなっていました。失敗したときだけ、
  少し待って試し直します（無効という答えが返ったときは試し直しません）。
- **読み上げ中の帯が、行の形と合っていなかったのを直しました。** 読んでいる文が
  **選択された状態**になっていて、OS の選択色の帯が、こちらが描く帯とは別に重なって出ていました。
  その帯は行の箱いっぱいに広がるので、文字の並びに対して左右へずれ、文字が終わった後ろまで伸び、
  ルビのある側へ膨らみ、2 行にまたがるとつながって見えていました。読み上げ位置へ画面を送るときに
  「選択にする」指定を渡していたのが原因です（プロトタイプ側は渡していませんでした）。
- 読み上げの帯から、ルビの読みの分を除きました。ルビの付いた文字のところだけ矩形が二重になり、
  半透明の帯が重なって、その箇所だけ色が濃くなっていました。
- 読み上げの帯の太さを揃えました。1 行に収まる文でも、2 行にまたがる文でも同じ太さになります。

## 0.4.0 — 2026-08-14（最初の公開版）

### 削除

- **PDF 対応を取り下げました。** このアプリは PDF ファイルをまったく受け付けなくなりました。
  取り込みの絞り込み・ファイル選択のダイアログ・Finder のファイル関連付けから外し、
  同梱していた pdf.js も削除しました。

  取り下げた理由は、**設計を間違えたから**です。PDF を表示する部分を別に作らず、
  EPUB を表示する仕組みの中に無理やり組み込みました。PDF の 1 ページを EPUB の 1 章に
  見立て、目次や表紙のデータを偽物で埋めて、EPUB と同じ入口に渡していました。
  そのため、EPUB 用の段組み・ページ送り・章の読み込みが、そのまま PDF にも使われました。
  この細工のために書いたコードは、テスト用の足場を入れて 400 行ほどになりました。
  そして、表紙が取れない・本文を検索できない・ページが白いまま・何冊か開くと固まる、
  という不具合が同時に起きました。以前ここに書いていた「絵を描く処理が終わらない」は、
  その中の 1 つの症状であって、原因ではありません。

  正しいやり方は、窓の上に PDF 用の層を重ねて、PDF を表示するライブラリにそのまま
  渡すことでした。確かめるために別のプロジェクトとして PDF リーダーを作ったところ、
  あっけなく表示できました。ですから、ここにあった PDF 用のコードは直して使うもの
  ではなく、丸ごと捨てるものだと判断しました。**受け付けない方が、使う人には親切です。**

  取り下げたことで 3 つ小さくなりました。配布物が 13 MB 減りました（pdf.js だけで
  追跡ファイル全体の約 7 割を占めていました）。第三者への帰属表示が 4 件不要になりました
  （pdf.js・Adobe CMap・Liberation フォント・Foxit フォント）。
  foliate-js への改変が 3 件から 1 件へ減りました。

  もう一度 PDF を扱うときは、このアプリの表示の仕組みに組み込み直すのではなく、
  別の窓・PDF 専用のライブラリで作ります。ですからこの削除のコミットは記録であって、
  取り消して戻すためのものではありません。

### 追加

- **対訳を設定から使えるようにしました。** 0.3.0 では実装だけ入れて画面に出していませんでしたが、
  設定に「対訳」タブを置き、ツールバーの **あA** とメニューの **表示 > 対訳を開く / 閉じる**
  から開けるようにしました。
- **OpenAI 互換 API のキー認証に対応。** 接続先 URL と API キーを設定画面で指定できます。
  手元のサーバー（LM Studio・Ollama 等）でも、クラウドのサービスでも同じ経路で使えます。
- 訳のキャッシュの件数表示と消去を設定に追加しました。
- **本を読んでいる最中でも、ウィンドウに本をドロップして取り込めるようにしました。** 今までは
  書棚の画面でしか受け付けませんでした。1 冊だけドロップしたときは、取り込んだあと**その本を
  そのまま開きます**。複数のファイルやフォルダをドロップしたときは、取り込んで書棚に留まります
  （どれを開けばよいか決められないためです）。
- **目次を枝ごとに折りたためるようにしました。** 見出しが 3 階層ある本では、目次が長くなって
  読みたい章まで送る手間がありました。子を持つ項目に三角を付け、開閉できるようにしています。
  開くのは今読んでいる章の親だけで、**自分で開いた枝は閉じません**。読んでいる最中に一覧が
  動くと使いにくいためです。

### 修正

- **考えてから答えるモデルに翻訳させると、何も返ってこなかったのを直しました。**
  `chat_template_kwargs` も `reasoning_effort` も `/no_think` も効かず、与えたトークンを
  全部思考に使い切っていました。思考を済ませたことにして書かせる方式に変えたところ、
  同じ段落・同じモデルで **90 秒かけて空 → 2 秒で訳文**になりました。
- 日本語以外へ訳したとき、モデルが付ける「English:」のような前置きが剥がれずに残っていたのを
  直しました。
- 訳のキャッシュの鍵に原文の言語が入っておらず、原文の言語を変えても前の訳が出ていたのを
  直しました。
- **絵だけのページで、絵が窓の半分しか使わず片側に寄っていたのを直しました。** 窓が
  1099 × 696 でも、絵の箱が 696 × 696 の正方形になり、403 ピクセルがいつも空いていました。
  縦書きでは右端、横書きでは左端に寄ります。絵の箱の大きさに段（カラム）の幅を使っていたのが
  原因です。段の幅は窓の高さと同じになるので、横長の窓では正方形になっていました。
- **対訳ペインの行をダブルクリックしても何も起きなかったのを直しました。** 元は本文を
  その段落へ飛ばす作りでしたが、対訳ペインの行は**いま見えている範囲の段落だけ**から
  作るので、飛び先が必ず画面の中でした。飛ばすのをやめ、**本文の該当段落を 1.8 秒だけ
  光らせる**ようにしました。ページを移ると印は消えます。

### 説明の訂正

- **README の「MOBI / AZW / AZW3 / KF8 は未検証」を書き換えました。** 実際に 4 つとも
  作って開いたところ、4 つとも開きました。`.azw3` と `.kf8` は縦書きのまま出ます。
  `.mobi` と `.azw` は横書きになります。古い MOBI 形式に縦書きを書く場所が無いためです
  （ツールバーの縦横ボタンで縦書きに直せます）。

## 0.3.0 — 2026-08-07

人に使ってもらうことを想定した最初の版。

### 追加

- **書棚を分ける。** 蔵書・分類・読書位置がそれぞれ独立した書棚を複数持てます。書棚を
  消してもデータは消さず `Deleted/` へ退避します。
- **分類とお気に入り。** 入れ子にできる分類、本のメニューからの割り当て、「未分類」の絞り込み。
- **フォルダ取り込み。** フォルダを選ぶと配下の本を再帰的に取り込みます。進捗表示と中止つき。
- **自動ページ送り。** 一定間隔でページを送ります。読み上げ中と手動送りの直後は譲ります。
- **スリープタイマー。** N 分後に読み上げを止め、続けて Mac をスリープ／シャットダウンする
  ことも選べます。シャットダウンは 30 秒の猶予を挟み、取り消せます。
- **対応形式を拡大**: EPUB に加えて CBZ・FB2・FBZ・Kindle 系（MOBI/AZW/AZW3/KF8）。
  Finder でダブルクリックした本が開きます。
- **書き出し**: いま開いている章を音声として、または本文を出した動画として書き出せます。
- **対訳ペイン**: OpenAI 互換のローカルサーバーで翻訳します。
- すべての機能にネイティブメニューバーから名前で届きます。
- 表紙の無い本に代替表紙。書棚の地には「続きの本」の表紙を敷きます。

### 変更

- 縦書き / 横書き / RTL を、OPF の宣言だけでなく**実際に組まれた向き**で決めるようにしました。
  矢印キー・タップゾーン・進捗スライダーが揃って同じ答えに従います。
- 画像だけの面（表紙・口絵）を、元の大きさのまま浮かせるのではなく版面いっぱいに描きます。
- 画面デザインを、移植元の Swift プロトタイプに合わせて作り直しました。

### 修正

- **スリープタイマーを書棚から操作できるようにしました。** 書棚でもメニュー項目は押せる
  状態だったのに何も起きず、しかも本を閉じるとタイマー自体が消えていました。書棚と
  リーダーの行き来をまたいで走り続け、どちらからでも解除できます。
- 無くなったメニュー項目（`file.dict`）に対する死んだハンドラを削除しました。
- 縦書き多段組でカラオケ式ハイライトの塗りが残る不具合を直しました。

### セキュリティ

- **配布ビルドでテストバスを無効にしました。** テストバスはローカルのポートを長ポーリングし、
  届いたものをアプリの権限で実行します。配布版で開けたままだと、先にそのポートを掴んだ
  ローカルのプロセスに画面キャプチャや任意パスへの書き出しをさせられてしまいます。
  デバッグビルドか `EPUB_READER_TESTBUS=1` を付けたときだけ有効になります。
  `capture_window` も同じ条件で塞ぎました。
- CSP をページの `<meta>` だけでなく `tauri.conf.json` にも書きました。Tauri 自身が
  描くものにも効くようになります。

### 配布

- `scripts/package-macos.sh` が、ビルド → ad-hoc 再署名 → dmg と zip の作成 →
  受け取る人と同じ展開の仕方での検証、までを通します。Tauri がビルド時に付ける署名は
  リソースが未封印で、素の `ditto` zip は AppleDouble を紛れ込ませて `unzip` 展開時に
  署名シールを壊します。このスクリプトはその両方を捕まえるためにあります。
- `LICENSE`（BSD-3-Clause）と `THIRD_PARTY_LICENSES.md` を追加しました。同梱物の全件と、
  foliate-js に加えた改変を明記しています。
- `test-books/` の見本画像を、出所のはっきりしない写真から
  `scripts/make-sample-images.py` が生成するものへ差し替えました。

## 0.2.0 — 2026-07-27（未公開）

仕様書に沿って表示・辞書・翻訳まわりを移植。

## 0.1.0 — 2026-07-26（未公開）

最初のビルド。書棚、縦書き表示のリーダー、VOICEVOX による文ハイライト追従つき読み上げ、
読み上げ辞書。
