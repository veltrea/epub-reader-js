# Third-party licenses

This repository bundles the components below. Each is used under its own
license; the project's own MIT terms (see [LICENSE](LICENSE)) do not
apply to them. Full license texts are shipped alongside the code at the paths
given here.

日本語版は [THIRD_PARTY_LICENSES.ja.md](THIRD_PARTY_LICENSES.ja.md)。

---

## foliate-js — MIT (modified)

- **Path**: `src/foliate-js/`
- **Upstream**: <https://github.com/johnfactotum/foliate-js>
- **Copyright**: (c) 2022 John Factotum
- **License text**: [`src/foliate-js/LICENSE`](src/foliate-js/LICENSE)

**This copy has been modified.** The MIT license permits modification; the
changes are recorded here and marked in the source with the comment
`※ foliate-js 本家からのローカル改変`:

| File | Change |
|---|---|
| `paginator.js` | Image-only pages (covers, frontispieces) get the full page box: column gap and size caps are dropped for that section, and `fillPageWithImage()` sizes the image to the page rect. The shadow-DOM layout CSS also moved from a `<style>` element to a constructed stylesheet (`adoptedStyleSheets`), because Tauri adds a nonce to the CSP, which blocks `<style>` elements created at runtime. Six marked sites. |
| `quote-image.js` | Shadow-DOM CSS moved to a constructed stylesheet, for the same reason as `paginator.js`. One marked site. |
| `view.js` | The branch for a document format this app no longer accepts was deleted, together with the helper that detected it. Two marked sites. |
| `tts.js` | Added `rangeOf()`, which reads the range of a sentence by its mark name without moving the highlight. The read-aloud dictionary needs the character offset of a sentence within its section, and upstream `setMark()` moves the highlight. One marked site. |
| `epub.js` | Added the legacy `<meta name="primary-writing-mode">` value to the metadata. Upstream does not read it. It is the last hint of writing direction for vertical books whose stylesheet lost its `writing-mode`. One marked site. |

Everything else in `src/foliate-js/` is upstream and unmodified.

## zip.js (@zip.js/zip.js) — BSD-3-Clause

- **Path**: `src/foliate-js/vendor/zip.js`
- **Upstream**: <https://github.com/gildas-lormeau/zip.js>
- **Copyright**: (c) 2022 Gildas Lormeau
- **License text**: <https://github.com/gildas-lormeau/zip.js/blob/master/LICENSE>

The bundled file is a minified rollup build made by foliate-js
(`src/foliate-js/rollup.config.js`); minification stripped the license header,
so the attribution is recorded here.

## fflate — MIT

- **Path**: `src/foliate-js/vendor/fflate.js`
- **Upstream**: <https://github.com/101arrowz/fflate>
- **Copyright**: (c) 2023 Arjun Barrett
- **License text**: <https://github.com/101arrowz/fflate/blob/master/LICENSE>

Same situation as zip.js: minified by foliate-js's rollup build, header stripped.

## Rust crates

The Rust side (`src-tauri/`) links Tauri and its dependency tree, which are
MIT/Apache-2.0. The exact set and versions are pinned in
[`src-tauri/Cargo.lock`](src-tauri/Cargo.lock); regenerate the full list with:

```bash
cargo install cargo-about && cargo about generate about.hbs
```

---

## Not bundled

These are **separate programs the app talks to over HTTP**. They are not
included in this repository or in the built app, and you must install them
yourself under their own terms:

- **VOICEVOX** — <https://voicevox.hiroshiba.jp/>
- **AivisSpeech** — <https://aivis-project.com/>
- **LM Studio** or any OpenAI-compatible server (optional, for translation)

Audio generated by these engines carries the terms of the individual voice
library you use — most require crediting the character's name when you publish
the result. Check the terms of the voice you pick before publishing audio or
video exported from this app.

## Sample books and images

Everything under `test-books/` and `src/samples/` was generated by the scripts
in this repository (`make_*.py`, `scripts/make-sample-images.py`,
`scripts/make-sample-book.py`) and is covered
by the project's own license. No third-party books, photographs, or fonts are
included there.
