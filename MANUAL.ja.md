**他の言語で読む:** [English](MANUAL.md)

# 電子書籍リーダー 操作マニュアル

日本語対応の電子書籍リーダー + 高品質読み上げ（VOICEVOX / AivisSpeech）アプリの使い方です。
**macOS 専用**です（メニューバー・Mac のスリープ / シャットダウン連携が macOS の機能に依存します）。
縦書き（右→左ページ送り）・横書きの両方に対応しています。

## 目次

1. [はじめに](#1-はじめに)
2. [セットアップ](#2-セットアップ)
3. [書棚](#3-書棚)
4. [リーダー](#4-リーダー)
5. [読み上げ](#5-読み上げ)
6. [対訳](#6-対訳)
7. [書き出し（音声・動画）](#7-書き出し音声動画)
8. [データの保存場所](#8-データの保存場所)
9. [トラブルシューティング](#9-トラブルシューティング)
10. [クレジットと利用条件](#10-クレジットと利用条件)

## 1. はじめに

<!-- fmt:azw fmt:azw3 fmt:cbz fmt:epub fmt:fb2 fmt:fbz fmt:kf8 fmt:mobi -->
- **対応形式**: EPUB（リフロー型・固定レイアウト FXL）、CBZ、FB2 / FBZ。
  - **これ以外の形式は受け付けません。** ウィンドウへ落としても何も起きませんし、
    ファイル選択のダイアログにも出てきません。
    力を入れているのはリフロー型の EPUB です。
  - MOBI / AZW / AZW3 / KF8 も開きます（Calibre で作った本で確認済み）。ただし
    **MOBI と AZW は縦書きの本でも横書きで出ます**。古い MOBI 形式には縦書きの指定を
    入れる場所が無いためです。ツールバーの「本文の向き」で縦書きに直せます。
    AZW3 と KF8 なら縦書きのまま出ます。
- **読み上げ**: VOICEVOX または AivisSpeech と連携し、高品質な音声で本文を読み上げます。
  読み上げ中の文はハイライトされ、読み上げ済みの部分が濃く塗られていきます（カラオケ式）。
- **対訳**: LM Studio（ローカル LLM）と連携し、いま表示している段落の訳文を横に並べて表示します。
- 読み上げ・対訳を使わない場合、外部ソフトは不要です。

<!-- group:setup -->
## 2. セットアップ

### アプリの起動

`epub-reader.app` をダブルクリックして起動します。最初に表示されるのが「書棚」です。

### 読み上げエンジンの準備（読み上げを使う場合のみ）

1. [VOICEVOX](https://voicevox.hiroshiba.jp/)（ポート `:50021`）または
   [AivisSpeech](https://aivis-project.com/)（ポート `:10101`）をインストールして**起動しておきます**。
2. アプリの「設定」（歯車アイコン）→「読み上げエンジン」で使うエンジンを選びます。
3. 「エンジン状態」が「接続済み」になっていれば準備完了です。「テスト再生」で声を確認できます。

### 翻訳の相手の準備（対訳を使う場合のみ）

対訳は **OpenAI 互換の API** に繋いで動きます。手元の機械で動かすサーバーでも、
クラウドのサービスでも、接続先を書けば同じように使えます。

**手元で動かす場合**（本文が外に出ません）

1. [LM Studio](https://lmstudio.ai/) や [Ollama](https://ollama.com/) を入れ、翻訳に使うモデルを読み込みます。
2. サーバーを起動します（LM Studio の既定は `:1234`、Ollama は `:11434`）。
3. 「設定」→「対訳」で接続先 URL を入れ、「接続状態」が緑になったらモデルを選びます。
   モデルを空のままにすると、一覧の先頭を使います。API キーは空で構いません。

**クラウドの API を使う場合**

1. 同じ「対訳」タブに、そのサービスの OpenAI 互換エンドポイントと API キーを入れます。
2. **本文がそのサービスへ送られます。** 未発表の原稿を訳すときは、送り先の規約を確かめてください。

どちらの場合も、翻訳の相手は**同梱していません**（[10 章](#10-クレジットと利用条件)）。

エンジンはいずれも**同梱していません**。各自でインストールしてください（[10 章](#10-クレジットと利用条件)）。

<!-- group:shelf -->
## 3. 書棚

アプリ起動時の画面です。取り込んだ本が一覧表示されます。

### 本の取り込み

- **「本を追加」ボタン** … 「ファイルを選ぶ…」または「フォルダから追加」を選びます。
  フォルダを選ぶと、その中（サブフォルダも含む）の本をまとめて登録します。
<!-- feat:reader-drop -->
- **ドラッグ & ドロップ** … 本のファイルやフォルダをウィンドウにドロップしても取り込めます。
  **本を読んでいる最中でも落とせます。** 1 冊だけ落としたときは、取り込んだあと
  **その本をそのまま開きます**。複数のファイルやフォルダを落としたときは、取り込んで
  書棚に留まります（どれを開けばよいか決められないためです）。
  既に取り込んである本を落としたときは、取り込みを飛ばしてその本を開きます。
- 取り込むと表紙サムネイルとタイトル・作者が表示されます。
  ※固定レイアウト（写真集など）の本は、表紙の抽出に数十秒かかることがあります。
- まとめて取り込んでいる間は進捗が出ます。「中止」でいつでも止められます（そこまでに
  登録した本は残ります）。

### 一覧の操作

| 操作 | 方法 |
|---|---|
| 本を開く | 表紙をクリック |
| 絞り込み | 上部の「フィルタ」に入力（対象は「すべて / タイトル / 作者 / 出版社」から選択） |
| 並び替え | 「最近開いた」「タイトル」「作者」「出版社」から選択 |
| 表示モード | 「グリッド」/「作者別」を切替。作者別はあ行〜わ行 + 「他」で分類 |
| 読み（よみ）の編集 | 本を**右クリック** → 「読みを編集」。五十音分類・タイトル順の並びに反映 |
| お気に入り | 右クリック →「お気に入りに追加」。サイドバーの「お気に入り」に集まります |
| 削除 | 本のメニューから「削除」（確認ダイアログあり） |

読み（yomi）は本のメタデータから自動推定されますが、漢字タイトルなどで分類が「他」に
なってしまう場合は右クリックから手動で設定してください。

### 分類（コレクション）

サイドバーで本をグループ分けできます。分類は**入れ子にできます**（下位の分類を作れます）。

- 「分類を追加」で作成、本を右クリック →「分類に入れる」で登録します。
- 1 冊を複数の分類に入れられます。「分類から出す」で外します。
- 分類を消しても**本は消えません**。下位の分類は一つ上の階層へ移ります。
- サイドバーの「すべて」「お気に入り」「未分類」で全体を切り替えられます。

### 書棚を分ける

蔵書そのものを複数に分けられます（「仕事用」「趣味用」など）。
メニューの **ファイル > 新規書棚… / 書棚を切り替える / 書棚を管理…** から操作します。

- 書棚ごとに分かれるもの: **蔵書・分類・お気に入り・読書位置・しおり・読み上げ辞書・共通CSS**
- 書棚をまたいで共通のもの: アプリの設定（テーマ・文字サイズ・言語・読み上げエンジン等）
- **EPUB ファイルの実体は共有**されます。書棚を削除してもファイルは消えません
  （データは `Deleted/` に残ります）。
- 新しい書棚は空の状態で始まります（同梱サンプルも入りません）。

<!-- group:reader -->
## 4. リーダー

本を開くと表示される画面です。縦書きの本は自動的に縦組み・右→左ページ送りになります。

### ツールバーの出し方（最初に知っておくこと）

本を開くと、**本文だけが表示されて操作ボタンは見えません**。本文をできるだけ広く使うためです。

- **マウスポインタを画面の上端または下端に寄せる**と、上下のツールバーが現れます。
  上には「書棚へ戻る・目次・表示設定」、下には「進捗スライダー・読み上げ操作」があります。
- ポインタを本文へ戻すと、また隠れます。
- ツールバーを出さなくても、**メニューバーからすべての機能に届きます**。
  書棚へ戻るのは **⌘L**、目次は **⌘T**、本文検索は **⌘F** です。

### ページ送り

| 方法 | 動作 |
|---|---|
| 画面の左右端をクリック（タップゾーン） | その方向へ 1 ページ |
| ツールバーの「前へ」「次へ」ボタン | 1 ページ移動 |
| ← / → キー | その方向へ 1 ページ（縦書き RTL でも見た目どおりの方向） |
| マウスホイール | 1 ノッチで 1 ページ |
| 下部の進捗スライダー | 本全体の任意の位置へジャンプ（縦書きの本ではスライダーも右→左） |

読書位置は自動保存され、次に開いたとき復元されます。

### 自動ページ送り

一定の間隔でひとりでにページを送ります（手が離せないときの流し読み用）。
メニューの **移動 > 自動ページ送り** から操作します。

- 間隔のプリセットは **10 / 15 / 20 / 30 / 45 / 60 / 90 秒**。「間隔を指定…」で
  1〜3600 秒の任意の値を入れられます。
- 動作中は画面に残り秒数が出ます。自分でページを送ると、その時点からカウントし直します。
- 止めるときは **移動 > 自動ページ送りを停止**。

### ツールバーの機能

| ボタン | 機能 |
|---|---|
| 書棚へ（←） | 書棚に戻る（読み上げ中なら停止） |
| 目次 | 章の一覧を表示してジャンプ。**見出しに子がある本では三角が付き、押すと枝を開閉できます**（下記） |
| 検索（🔍） | 本文の全文検索。ヒット件数表示・クリックでその場所へ |
| 対訳 | 訳文のペインを開く / 閉じる（[6 章](#6-対訳lm-studio)） |
| カスタムCSS | 本文の見た目を CSS で上書き。「全書籍共通CSS」と「この本だけのCSS」の 2 欄（入力で即反映） |
| 余白 | 端いっぱいまで文字が詰まる本に強制的に余白を付ける |
| 本文の向き | 自動（本の指定）→ 縦書き（強制）→ 横書き（強制）を押すたびに切り替え。強制中はアイコンが色付き（下記） |
| A− / A+ | 文字サイズの縮小 / 拡大（`-` / `+` キーでも可） |
| しおり | 「現在地をしおり」で登録、一覧から復帰 |
| 見開きをずらす | 固定レイアウトの本でのみ表示。見開きのペアが 1 ページずれている本を補正（下記） |
| 設定（⚙） | テーマ・文字サイズ・行間・読み上げ・対訳・言語などの設定 |

<!-- feat:toc-fold -->
### 目次の折りたたみ

見出しが「部 → 章 → 節」のように何段にも分かれている本では、目次が長くなります。
そこで、**子を持つ項目には三角が付き、押すと枝を開閉できます**。

- 三角を押すと開閉します。**文字のほうを押すとその章へ飛びます**（子を持つ見出し自体にも飛べます）。
- 子を持たない項目には三角が出ませんが、場所だけは空けてあるので行頭が揃います。
- 章が変わると、**いま読んでいる章までの枝は自動で開きます**。強調された行が隠れないためです。
- **自分で開いた枝は、章が変わっても閉じません。** 目次を触っている最中に一覧が動くと使いにくいためです。

### 縦書き / 横書きの強制

本文の向きは通常 EPUB 側の指定（`writing-mode`）に従いますが、指定が誤っている本や、
好みで向きを変えたい場合に強制できます。

- ツールバーの「本文の向き」ボタンで **自動 → 縦書き（強制）→ 横書き（強制）** と切り替わります。
- 設定モーダルの **「本文の向き」** でも同じ値を選べます（設定は全書籍共通・自動保存）。
- 切り替えても本を開き直さないので、**読んでいた位置はそのまま**です。
- 向きに合わせてページ送りの左右も入れ替わります（縦書き＝右→左、横書き＝左→右）。
  進捗スライダーの向きも連動します。
- ルビや `text-combine-upright`（縦中横）の指定はそのまま残ります。
- 固定レイアウトの本（写真集・漫画）ではページ画像そのものが向きを持つため、
  このボタンは表示されません。

<!-- set:autoPagerSeconds set:binding set:fontScale set:forceMargin set:imageSpread set:lang set:lineHeight set:renderMode set:shelfView set:sortKey set:textSpread set:theme set:userCSS set:writingMode -->
### 表示の設定（設定モーダル）

- **テーマ**: 自動 / ライト / セピア / ダーク
- **本文の向き**: 自動（本の指定に従う）/ 縦書き（強制）/ 横書き（強制）
- **綴じ方向**: 自動 / 右綴じ / 左綴じ
- **見開き**: 画像ページ・本文それぞれに 自動 / 常に見開き / 常に単ページ
- **文字サイズ**・**行間**: リフロー型の本に適用
- **言語**: アプリの表示言語（日本語 / English、既定は自動判定）

### メニューバー（macOS）

画面上部のメニューバーも**言語設定に追従**します。日本語なら日本語、それ以外の言語では
英語で表示されます（既定の「自動」では OS の表示言語で判定）。設定を変えると即座に組み直
されます。その画面で使えない項目はグレーアウトします。

<!-- menu:file.import menu:file.importFolder menu:file.newProfile menu:file.profiles menu:file.saveAudio menu:file.saveVideo menu:tts.dict -->

**ファイル**

| 項目 | 説明 | ショートカット |
| --- | --- | --- |
| 開く… | 本を追加（ファイル選択） | ⌘O |
| フォルダから追加… | フォルダの中の本をまとめて登録 | |
| 書き出し | この章を音声ファイル / 朗読動画に保存（[7 章](#7-書き出し音声動画)） | |
| 書棚を切り替える / 書棚を管理… / 新規書棚… | 書棚の分割（[3 章](#書棚を分ける)） | |
| 読み上げ辞書… | 誤読の修正（[5 章](#読み上げ辞書誤読の修正)） | |

<!-- menu:view.aspect menu:view.bookmarkAdd menu:view.bookmarks menu:view.css menu:view.fontDec menu:view.fontInc menu:view.fontReset menu:view.lineDec menu:view.lineInc menu:view.lineReset menu:view.margin menu:view.render.friendly menu:view.render.raw menu:view.spread menu:view.theme.auto menu:view.theme.dark menu:view.theme.light menu:view.theme.sepia menu:view.toc menu:view.translate -->

**表示**

| 項目 | 説明 | ショートカット |
| --- | --- | --- |
| 目次 | 章の一覧 | ⌘T |
| しおり / 現在地をしおり | しおりの一覧 / 追加 | ⌘B / ⌘D |
| 書字方向 | 自動 / 縦書き / 横書き | |
| 綴じ方向 | 自動 / 右綴じ / 左綴じ | |
| 画像ページの見開き / 本文の見開き | 自動 / 常に見開き / 常に単ページ | |
| 見開きをずらす | 固定レイアウトのページの組がずれている本の補正 | |
| 画像の比率… | 画像ページを版面に合わせる比率の指定 | |
| 余白を切り替え | 端まで文字が詰まる本に余白を足す | |
| 表示モード | 「読みやすさ優先」（実在の本を無難に表示）/「EPUB のまま」（指定どおりに描く） | |
| カスタムCSS… | 全書籍共通 / この本だけ の 2 層 | |
| 文字を大きく | 文字サイズの拡大 | ⌘= |
| 文字を小さく | 文字サイズの縮小 | ⌘- |
| 文字サイズを標準に戻す | 既定値へ | ⌘0 |
| 行間を広く | 行間の拡大 | |
| 行間を狭く | 行間の縮小 | |
| 行間を標準に戻す | 既定値へ | |
| 対訳を開く / 閉じる | 訳文のペイン（[6 章](#6-対訳lm-studio)） | |
| フルスクリーン | 全画面表示 | |

<!-- menu:app.settings menu:edit.find menu:go.autoPager.custom menu:go.autoPager.stop menu:go.next menu:go.prev menu:go.shelf menu:tts.play menu:tts.sleep.action.shutdown menu:tts.sleep.action.sleepSystem menu:tts.sleep.action.stopOnly menu:tts.sleep.cancel menu:tts.sleep.custom menu:tts.stop -->

**移動 / 読み上げ / そのほか**

| 項目 | 説明 | ショートカット |
| --- | --- | --- |
| 前のページ / 次のページ | ページ送り | ⌘← / ⌘→ |
| 自動ページ送り | 間隔のプリセットと停止（[4 章](#自動ページ送り)） | |
| 書棚へ戻る | 本を閉じる | ⌘L |
| 読み上げ / 一時停止 | 読み上げの開始・一時停止 | ⌘R |
| 読み上げを停止 | 停止 | ⌘. |
| スリープタイマー | 満了時の動作と時間（[5 章](#スリープタイマー)） | |
| 本文を検索… | 全文検索 | ⌘F |
| 設定… | 設定モーダル | ⌘, |

### 固定レイアウト（写真集・漫画）の見開き

- 見開き 2 ページは中央で継ぎ目なく結合表示されます。横長 1 枚の絵を左右 2 ページに
  分割してある本も、正しいペアなら 1 枚の絵として表示されます。
- **絵が半分ずつずれて表示される場合**（表紙の枚数などでペアがずれている本）は、
  ツールバーの「見開きをずらす」を押すと 1 ページずれて正しいペアになります。
  設定は本ごとに保存されます。

<!-- group:tts -->
## 5. 読み上げ

### 基本操作

| 操作 | 方法 |
|---|---|
| 読み上げ開始 | ▶ ボタン、または **スペースキー** |
| 一時停止 / 再開 | 同じボタン（▶ / ⏸）またはスペースキー |
| 停止 | ⏹ ボタン |
| 途中から読む | 本文の読みたい位置を**ダブルクリック** |

- 読み上げ中は現在の文がハイライトされ、画面が自動で追従します。ページ末・章末も
  自動で送られ、本の最後まで連続して読み上げます。
- 次に読む文は再生中にバックグラウンドで先行合成されるため、文と文の間の待ち時間は
  ほとんどありません。
- ルビ（ふりがな）は視覚表示のまま、読み上げでは重複して読まれません。

### スリープタイマー

寝る前に読み上げを掛けたまま眠るための機能です。メニューの
**読み上げ > スリープタイマー** から操作します。

- 時間のプリセットは **15 / 30 / 45 / 60 / 90 / 120 分**。「時間を指定…」で任意の分数を
  入れられます。動作中は残り時間が画面に出ます。
- **満了時の動作**は 3 択です。どれを選んでも読み上げの停止は必ず行われます。
  - **読み上げを停止** … 停止するだけ（既定）
  - **Mac をスリープ** … 読み上げを止めたあと Mac をスリープさせます
  - **Mac をシャットダウン** … 読み上げを止めたあと Mac をシャットダウンします
- **シャットダウンだけは 30 秒の猶予**を挟みます。その間に「取り消す」を押せば
  シャットダウンしません（「いますぐ」で猶予を待たずに実行）。目が覚めていたときに
  勝手に電源が落ちないようにするためのものです。
- 「タイマーを解除」でいつでも取り消せます。
- タイマーはアプリを終了すると消えます（翌日の起動で生き返って Mac を落とす、という
  事故を防ぐため）。

> **注意**: 「Mac をスリープ」「Mac をシャットダウン」は、このアプリだけでなく
> **Mac 全体**を止めます。保存していない他のアプリの作業がある状態で使わないでください。
> シャットダウンは macOS の System Events 経由で要求するため、他のアプリが終了を
> 拒否した場合はそこで止まります。

<!-- set:customBaseUrl set:engine set:pauseLengthScale set:sleepTimerAction set:sleepTimerMinutes set:speaker set:speedScale set:ttsSaveDir -->
### 声の設定（設定モーダル）

- **読み上げエンジン**: VOICEVOX / AivisSpeech / カスタム URL
- **話者**: エンジンが提供する声の一覧から選択
- **話速**: 読み上げ速度
- **無音の長さ**: 改行・句読点での間の長さ

### 読み上げ辞書（誤読の修正）

固有名詞などをエンジンが誤読する場合に使います（書棚の「読み上げ辞書」ボタン）。

1. 「表記」に誤読される語、「読み(かな)」に正しい読みを入力して「追加」
2. 必要なら「優先度」（大きいほど優先）と「アクセント位置」を調整
3. 「エンジンに登録」を押すとエンジンのユーザー辞書に反映されます

辞書は読み上げ開始時にも自動で登録されます。読み上げ辞書は**書棚ごと**に分かれます。

<!-- group:trans -->
## 6. 対訳

いま画面に出ている段落を翻訳し、原文の横に並べて表示します。本文の組版には手を入れないので、
縦書き・見開き・ルビはそのままです。

- 開閉は ツールバーの **あA** ボタン、またはメニューの **表示 > 対訳を開く / 閉じる**。
- ページを送ると、その画面に見えている段落だけを翻訳します（1 画面あたり最大 40 段落）。
- 一度訳した段落はキャッシュされ、同じ場所へ戻ったときは訳し直しません。
- ペイン上部の **原** で原文の表示を消せます。**A－ / A＋** で訳文の文字サイズを変えられます。
<!-- feat:tr-flash -->
- **行をダブルクリックすると、本文の該当段落が 1.8 秒だけ光ります。** どの訳が本文のどこかを
  確かめるためのものです。ページを送ると印は消えます。

<!-- set:translation -->
### 設定（設定モーダル →「対訳」）

| 項目 | 既定 | 説明 |
|---|---|---|
| 接続先 URL | `http://127.0.0.1:1234` | OpenAI 互換 API のアドレス。手元でもクラウドでも |
| API キー | 空 | 手元のサーバーなら空のまま。クラウドはここにキーを入れます |
| モデル | 自動（一覧の先頭） | 接続先が返す一覧から選びます |
| 原文の言語 | 自動判定 | 翻訳元の言語 |
| 訳文の言語 | 日本語 | 翻訳先の言語 |
| ゆらぎ | 0.20 | 上げるほど訳が自由になり、下げるほど原文に忠実になります |
| 同時に訳す数 | 2（1〜8） | 増やすと速くなりますが、機械が重くなります |
| 前の段落を文脈として渡す | 有効 | 訳の一貫性が上がる代わりに送信量が増えます |
| 思考を打ち切る | 有効 | 下記参照。**手元のサーバーでは入れたままにしてください** |
| 訳のキャッシュ | — | 「消去」で全部捨てられます（段落数が表示されます） |

### 「思考を打ち切る」について

考えてから答えるモデル（推論モデル）は、1 段落の訳に思考を使い切って**何も返さない**ことが
あります。この設定を入れると、思考を済ませたことにして訳文から書かせます。実測では、
同じ段落・同じモデルで **90 秒かけて空 → 2 秒で訳が出る**ほど違いました。

手元のサーバーでは入れたままにしてください。クラウドの API に断られる場合だけ外します。

### 訳の質について

訳の質は、**選んだモデルで決まります**。アプリは訳を作らず、受け取ったものを並べるだけです。

- 小さいモデルほど、会話・敬語・固有名詞で崩れます。人名を勝手に作ることもあります。
- **向きによって難しさが違います。** 日本語から英語へ訳すほうが、英語から日本語へ訳すより
  小さいモデルでうまくいきます。日本語を「書く」ほうが「読む」より難しいためです。
- 読めない言語へ訳したものは、**そのままでは正しさを確かめられません**。人に見せるもの、
  仕事に使うものは、その言語が読める人か、より大きなモデルで確かめてください。

### 訳が出ないとき

- 「設定」→「対訳」の**接続状態**を見てください。緑にならないなら、サーバーが起動しているか、
  URL が合っているか（クラウドならキーが正しいか）を確かめます。
- モデルが 1 つも無いと翻訳を始められません。
- 「モデルが考えるだけで訳文を返しませんでした」と出たら、「思考を打ち切る」を入れてください。

<!-- group:export -->
## 7. 書き出し（音声・動画）

読み上げている章を、ファイルとして保存できます。メニューの **ファイル** から操作します。

- **この章を音声ファイルに保存…** … 章全体を読み上げた音声を書き出します。
- **この章を朗読動画に保存…** … 本文の見た目と読み上げ音声を合わせた MP4 を書き出します。
  縦書き / 横書きに応じて動画の縦横を自動で決めます。
  書き出し中は進捗が出ます（「中止」で止められます）。

書き出しには章全体の音声合成が必要なため、長い章では時間がかかります。
書き出した音声・動画を公開する場合は、[10 章](#10-クレジットと利用条件)を確認してください。

<!-- group:data -->
## 8. データの保存場所

取り込んだ本・設定・しおり・辞書はすべて以下に保存されます:

```
~/Library/Application Support/dev.veltrea.epub-reader/
```

- `books/` … 取り込んだ本の実体（書棚をまたいで共有）
- `library.json` … 書棚（タイトル・作者・読み・表紙）
- `settings.json` / `dict.json` … 設定・読み上げ辞書
- `loc-*` / `bm-*` / `css-*` / `spread-*` … 本ごとの読書位置・しおり・CSS・見開きずらし
- `Deleted/` … 削除した書棚のデータ

このフォルダを削除するとアプリは初期状態に戻ります（取り込んだ本も消えます）。

## 9. トラブルシューティング

| 症状 | 対処 |
|---|---|
| 読み上げボタンを押しても声が出ない | エンジン（VOICEVOX / AivisSpeech)が起動しているか確認。設定の「エンジン状態」が「未接続」なら起動後に「保存」し直す。エンジンに届かないときは読み上げを止めて画面下に「読み上げエンジンに接続できません」と出ます |
| ウィンドウへ落としても何も起きないファイルがある | その形式に対応していません（[1. はじめに](#1-はじめに)の対応形式を参照）。対応していないファイルは、わざと無視しています。書棚には何も追加されません |
| 本を開いたら操作ボタンが消えた | 上下のツールバーは**マウスポインタを画面の上端・下端へ寄せると出ます**（本文を広く使うため、普段は隠れています）。キーボードだけで戻るなら **⌘L**（書棚へ戻る） |
| 固有名詞が誤読される | 「読み上げ辞書」に表記と読みを登録 → 「エンジンに登録」 |
| 対訳に訳文が出ない | LM Studio の Local Server が起動しているか、モデルが読み込まれているか確認 |
| 対訳が遅い | 設定で「同時に投げる段落数」を増やす。「直前の段落を文脈として渡す」を切ると軽くなります |
| 写真集の絵が左右半分ずつずれる | ツールバーの「見開きをずらす」を押す |
| 本文が端で切れて見える | ツールバーの「余白」を有効にする。またはカスタム CSS で調整 |
| 縦書きの本が横書きで表示される（またはその逆） | ツールバーの「本文の向き」で縦書き / 横書きを強制する |
| 五十音分類が「他」になる | 本を右クリック → 「読みを編集」でかなを設定 |
| 取り込みが終わらない | 固定レイアウトの大きい本は表紙抽出に数十秒かかることがあります。しばらく待ってください |
| MOBI / AZW の本が横書きで開く | 古い MOBI 形式に縦書きの指定を入れる場所が無いためです。ツールバーの「本文の向き」で縦書きにするか、AZW3 / KF8 に変換してから取り込んでください |

## 10. クレジットと利用条件

- **VOICEVOX / AivisSpeech は同梱していません。** 各自で入手してください。エンジン本体と
  音声ライブラリ（キャラクターの声）の利用条件は、**それぞれの配布元の規約**に従います。
- **書き出した音声・動画を公開・配布する場合、多くの音声ライブラリでキャラクター名の
  クレジット表記が必要です**（例: `VOICEVOX:四国めたん`）。表記の形式・条件は音声ライブラリ
  ごとに違うので、使った声の規約を必ず確認してください。商用利用の可否も声ごとに異なります。
- **LM Studio と翻訳モデルも同梱していません。** モデルごとのライセンス（出力の扱いを
  含む）は配布元の規定に従います。
- 本アプリ自体は、これらのエンジンに HTTP で接続するだけで、音声・モデルのデータを
  再配布していません。アプリのライセンスは [LICENSE](LICENSE) を参照してください。
- 表示エンジンに [foliate-js](https://github.com/johnfactotum/foliate-js) を使っています。
